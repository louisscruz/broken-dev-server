document.addEventListener('DOMContentLoaded', () => {
  const root = document.getElementById('root');
  const message = 'Success! Bundle.js found!';
  console.log(message);
  root.innerHTML = message;
});
